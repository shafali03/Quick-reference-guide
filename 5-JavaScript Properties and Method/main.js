

// Property is to get certain information about a object.
// Method are function which belong to the objects.


let item = "Bottle of water"
console.log(item.indexOf('of'));

// Property = .length number of character in the string = 15

// Method = indexOf() will give you the position of letter  (item.indexOf('of')); = 7;



// Method = substring() will give you the position of and the rest of the words will be cut off  (item.substring(7, 9)); = of; 

// Method = replace() With replace you can replace the word ie water to Juice (item.replace("water", "Juice")); = Bottle of Juice; 

// Method = item.toUpperCase() will turn the string to uppercase


let item = ["Bottle", 4, true]
console.log(item.join());

// Method = items.join() this will join the string together and get rid of white spaces


let item = ["Bottle", 4, true]
items.pop()
console.log(item.pop());

// will remove the last index array in this case it's true



let item = ["Bottle", 4, true]
items.push("Juice")
console.log(item);

// The push method will push Juice at the end of the array



let item = ["Bottle", 4, true]
items.push("Coke")
console.log(item.push("Coke"));
//This will show you the index of the array












